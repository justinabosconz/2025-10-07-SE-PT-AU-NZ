// function getGreeting(name) {
//   return "Hello " + name + "!";
// }

// FUNCTION EXPRESSION SYNTAX
let getGreeting = function (name) {
  return "Hello " + name + "!";
};

console.log(getGreeting("Justina"));

// ARROW FUNCTION SYNTAX
let getGreeting2 = (name) => "Hello " + name + "!";

console.log(getGreeting2("Justina"));
